# Environment

# Optional — LLM-authored SAT items via /api/generate-question
# Without this key, Ace still generates infinite procedural items offline.
OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
